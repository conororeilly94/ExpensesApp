<?xml version="1.0" encoding="utf-8"?>
<!-- fitsSystemWindows - makes your ListView fit to the edges of the action bar and navigation bar -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/drawer_layout"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    tools:context=".HomeActivity">

    <!-- include - inherits layout of another view -->
    <include layout="@layout/appbar_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

<!--    TextAppearance allows you to define text-specific styling while leaving a View 's style available for other uses.-->
    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:id="@+id/navbarView"
        app:itemTextColor="@color/app_head"
        android:fitsSystemWindows="true"
        app:itemTextAppearance="?android:textAppearanceSmall"
        app:itemIconTint="@color/toolbar_color"
        app:menu="@menu/navmenu"
        app:headerLayout="@layout/nav_header"
        />

</androidx.drawerlayout.widget.DrawerLayout>